<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header class="text-center text-yellow bg-balck">
        <div class="py-4">
            <a class="text-decoration-none text-yellow" href="/">
                <h2 class="cabecalho-titulo-h2">PETISCARIA</h2>
                <h1 class="cabecalho-titulo-h1">Bela Vista</h1>
            </a>
        </div>
    </header>

    <nav class="links">
        <a class="text-black text-decoration-none" href="cad-produtos">Cadastro</a>
        <a class="text-black text-decoration-none" href=""><strong>Produtos</strong></a>
    </nav>

    <main class="container">
       <section class="mt-3">
            <div class="row">
                {{#each produtos}}
                    <div class="col-lg-3 text-center">
                        <div class="border rounded-3 p-3 mb-4">
                            <figure>
                                <img src="{{dataValues.img}}" alt="">
                            </figure>
                            <p>ID: {{dataValues.id}}</p>
                            <p>Nome: {{dataValues.nome}}</p>
                            <p>Valor: {{dataValues.preco}}</p>
                            <p>Cadastrado: {{#formatDate dataValues.createdAt}}{{/formatDate}}</p>
                            <a href="/del-produto/{{dataValues.id}}"><button class="btn bg-yellow bg-yellow-opacity text-light">Excluir</button></a>
                        </div>
                    </div>
                {{/each}}
            </div>
        </section>
    </main>
</body>
